<h1>編集画面</h1>
<%= form_with model:@list, url:"/todolists/#{@list.id}", local:true do |f| %>
 <%#ここのurlの先頭に/つけないと、アドレスに無限ループが発生する。
 また、/todolists/#{@list.id}でリストから保存してあるやつをひっぱってきている。# %>
<h4>タイトル</h4>
<%= f.text_field :title %>
<h4>本文</h4>
<%= f.text_area :body %>
<%= f.submit "保存" %>
<% end %>